export default class Calculadora {

    static transformarCoordenadas(lat, lon, epsg_in, epsg_out) {

        console.log(`Tranformar la coordenada ${lat},${lon} del ${epsg_in} al ${epsg_out}`);

    }

}

